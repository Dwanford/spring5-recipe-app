spring:
  sql:
    init:
      platform: mysql
  datasource:
    url: jdbc:mysql://localhost:3306/recipe_app_prod
    username: recipe_prod_user
    password: Dwanford123
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false